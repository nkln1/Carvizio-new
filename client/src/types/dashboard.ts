import type { Request as RequestType } from "@shared/schema";

// Re-export the Request type for use in dashboard components
export type { RequestType as Request };

// Additional dashboard-specific types can be added here as needed
